这项目的代码编写都是ai-coding的，因为当时想着做个小agent项目，但是第一次接触。
所以我一开始就让ai使用langchain最新的架构，然后让他搭建一个雏形。一开始的项目是非常简单的，只有一些基本的功能
和ui界面及其简陋。后续陆陆续续就按照自己的想法（提供自己设计的算法思路，也让ai提供一些优化方案，让我抉择）慢慢去完善功能
在这之中真的学到好多东西。

我以前一直以为大模型是自己管理上下文的，我只需要提供问题就行。在做了之后，发现大模型是无状态的
需要用户本地管理上下文，我让ai查一下一开始的上下文管理是怎么样的（它就无脑叠加记录，我知道后我都心态我的token呜呜呜）
然后我就简单的根据使用时的逻辑，让模型按我的思路去压缩上下文，减少token的消耗。

然后呢，我又遇到了另一个问题，每次模型打开不就要全部读取文件内容才知道改哪里了？有一些文件都不需要读取，这不就浪费了？
我就开始搜索优化方案，然后看到ai提到的rag检索方案（结合我在boss上看到挺多岗位需要有rag经验，我就挺感兴趣的）
然后就开始添加rag检索的功能，我一开始还非常天真的觉得我加了这个，就能非常高效地推荐文件给大模型，提高效率
但现实是残酷的，简单的查向量相似度并不能准确的找到需要的文件（其实还是和使用者能否准确精准地提问，挺大关系地）
我就想着要去优化了，发现了一个reranking方案（当时在阿里云平台看到有reranking的模型，就选了这个方案）通过ai了解
不太需要调用模型，这样会时间消耗很高，简单的规则reranking就够了。简单的rerangking规则一开始固定死了top_k的输出
为5（这是非常不合理的）然后就改成动态的top_k设置。我想是不是应该有个评分（来区分是否有关）超过的都添加进去，同时这个
评分也应该是动态的，根据文件啥的动态去设置。现在最终版本的rag其实我也不是很满意的了，但我也不知道怎么去优化比较好了
只能说当问题非常明确的时候，感觉还是能用。当问的很宽泛，比如优化下整个项目的代码质量啥的。他就识别不出来，要给模型读
全部文件。后面我让ai对于这种情况进行了下优化，发送整个项目的文件树，让模型去调用。

在优化rag的同时，工具的构建和使用逻辑也是让我有点头疼。刚开始的ai给出的代码很呆的，模型一次就只能一个工具调用请求
修改多个文件，好多个请求调用，浪费资源。我当时在想，按照逻辑来说这应该都是可以连续调用的，然后询问ai是否可以这么做
，ai告诉我tool_call是支持的，我就让他改了。然后就开始完善一些文件编写的工具，一开始只有edit，delete，move这几个
很基础的。后面才开始完善复制，重命名，创建等工具，后面又完善了文件夹等工具。

在完善了这些工具后，我发现我原有的edit工具不太能用了，当文件内容很长时，模型一般都是返回需要修改的代码段（一个类或者
一个函数）但是我的edit工具是要让它放回全部工作内容。一开始我也想过偷懒，就这么做好了，在system_prompt中强制它返回
全部文件，但是效果不佳。我想想这么干确实不行，不太合理，还是要能做到cursor那种针对的修改。一开始我也不知道用啥方法去实现
比较好，问了ai，哪几个方案都感觉不合适。直到有一天我在看一篇公众号的推文，接触到了hashline这种方案，当时我就觉得
真的非常妙啊，直接对每行代码进行hash就行了，让模型返回需要修改哪些hash的内容就行了。然后就让ai帮我推荐一下hashline
的实现方案，最终我结合它给的多种方案的优点，把每个方案的优点都提出来弄了一个混合方案。对于类/函数这种代码块
进行块hash；对于独立语句，就进行（行+内容）的方式进行hash（这样避免有重复内容，同一个hash，然后加入偏移量，知道是
第几个）最后在完善一下多种编程语言的代码解析器（用来解析哪些是块hash，哪些独立语句）就这样它能用了，效果还不错。

大概内容就这些啦，其他的小修小补了。也算是边学边实践了，没接触过，也没学习过，所以都是ai编程，代码我是真没写几行。
加上在学校主力我使用的主力语言是c++,python的课是有修的，只能说大概的语法，代码阅读还是问题不大。但是具体的库之类的
没怎么用过，不太熟。
我基本全去指挥ai干活了，现在只能说是了解个大概，知道agent怎么个运行逻辑了，具体的框架实现还要后面好好琢磨了。
希望到时候有机会面试的话，面试官能轻点拷打了【🙏】

现在的ai编程是真厉害啊，从大二开始就有接触ai编程了，之前的ai编程的挺多bug，然后逻辑错误也挺多的。现在基本都能
能正确的输出代码了，但是还是得去review一下，检查下他的算法逻辑是否有错或者能够优化。我觉得就人提供算法思路
，框架构造等等东西，让ai去根据这个针对性的实现（这样效率应该蛮高的）
